node_modules/
.env
.env.*
!.env.example

# Uploaded user files — runtime data, never commit
uploads/

# Runtime data — never overwrite production
bookings.json
quotes.json
users.json
vehicles.json
server.pid
